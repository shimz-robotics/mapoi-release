# A single tag definition describing a POI classification tag
string name           # Tag identifier (e.g. "goal", "event")
string description    # Human-readable description of the tag's purpose
bool is_system        # True if this is a built-in system tag, false for user-defined tags
